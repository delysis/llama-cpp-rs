# Controlled-generation branch reconciliation

Canonical base: `01e48b7c1e7de39c3e5e8a67cd9efac498f8da1f`

Compared branch head: `a74dbb79f96e0ebad8b0737ee1d3c9c1deb185af`

This ledger is semantic. The stale branch is not merged or cherry-picked, and
the vendored llama.cpp revision is not moved.

| Unique stale commit | Disposition | Canonical evidence |
| --- | --- | --- |
| `fa5d5f164ab6abef521ee7f1280273b60535dc6f` | already-present | The live-pooling embedding-width fix is present through upstream merge `bed81ad`; `LlamaContext::pooling_type`, `LlamaContext::embeddings_seq_ith`, and the rank/non-rank width paths use the live context pooling type. `git cherry 01e48b7 a74dbb7` marks this patch equivalent. |
| `0adab1a3bb98d875cc15a7ffd0aee59043712b21` | intentionally-superseded | Canonical commit `01e48b7` restores the controlled-generation wrappers with stricter extracted validators and tests, plus stable native build evidence. LoRA stack validation, control-vector validation, logit access, sampler clone/error handling, top-n-sigma, eta cutoff, and FIM wrappers are retained. |
| `a74dbb79f96e0ebad8b0737ee1d3c9c1deb185af` | already-present | `LlamaLoraAdapter` documents model ownership, carries a model borrow, has the model-before-adapter compile-fail example, and is proven non-dropping by `adapter_handle_never_frees_model_owned_allocation`. `lora_adapters_set` documents retained non-owning context pointers and empty-stack clearing. |

## Review commands

```text
git log --reverse --format='%H %s' 01e48b7c1e7de39c3e5e8a67cd9efac498f8da1f..a74dbb79f96e0ebad8b0737ee1d3c9c1deb185af
git cherry 01e48b7c1e7de39c3e5e8a67cd9efac498f8da1f a74dbb79f96e0ebad8b0737ee1d3c9c1deb185af
git diff 01e48b7c1e7de39c3e5e8a67cd9efac498f8da1f a74dbb79f96e0ebad8b0737ee1d3c9c1deb185af -- llama-cpp-2/src/context.rs llama-cpp-2/src/lib.rs llama-cpp-2/src/model.rs llama-cpp-2/src/sampling.rs
```

## Result

No behavior remains exclusively on the stale branch. The only successor-tree
change required by this reconciliation is this machine-checked disposition
ledger; the canonical code already contains the LoRA lifetime correction and
the stronger wrapper/build-evidence implementation.
